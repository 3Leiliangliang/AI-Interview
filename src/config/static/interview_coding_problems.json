[
  {
    "id": "two-sum-index",
    "title": "两数之和（返回下标）",
    "source": "interview-seed",
    "summary": "给定整数数组和目标值，返回两个相加等于目标值的元素下标。",
    "description": "请实现一个函数 twoSum(nums, target)，在数组中找到两个和为 target 的元素，并返回它们的下标。默认每组输入只会有一个有效答案，且不能重复使用同一个元素。",
    "input_description": "nums 为整数数组，target 为目标整数。",
    "output_description": "返回长度为 2 的数组，包含两个满足条件的下标。",
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]"
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]"
      }
    ],
    "starter_code": {
      "javascript": "export function twoSum(nums, target) {\n  // TODO\n  return []\n}\n",
      "typescript": "export function twoSum(nums: number[], target: number): number[] {\n  // TODO\n  return []\n}\n"
    },
    "allowed_languages": ["javascript", "typescript"],
    "checks": {
      "javascript": {
        "required_snippets": ["const seen = new Map()", "seen.set", "return [seen.get(complement), index]"],
        "forbidden_snippets": ["return []"]
      },
      "typescript": {
        "required_snippets": ["const seen = new Map<number, number>()", "seen.set", "return [seen.get(complement)!, index]"],
        "forbidden_snippets": ["return []"]
      }
    }
  },
  {
    "id": "valid-parentheses",
    "title": "有效括号",
    "source": "interview-seed",
    "summary": "判断只包含括号字符的字符串是否有效闭合。",
    "description": "请实现 isValid(s)，如果字符串中的括号都能按正确顺序闭合则返回 true，否则返回 false。字符串只包含 ()[]{}。",
    "input_description": "s 为括号字符串。",
    "output_description": "返回布尔值。",
    "examples": [
      {
        "input": "s = '()[]{}'",
        "output": "true"
      },
      {
        "input": "s = '([)]'",
        "output": "false"
      }
    ],
    "starter_code": {
      "javascript": "export function isValid(s) {\n  // TODO\n  return false\n}\n",
      "typescript": "export function isValid(s: string): boolean {\n  // TODO\n  return false\n}\n"
    },
    "allowed_languages": ["javascript", "typescript"],
    "checks": {
      "javascript": {
        "required_snippets": ["const stack = []", "const pairs = {", "stack.pop()"],
        "forbidden_snippets": ["return false\n}"]
      },
      "typescript": {
        "required_snippets": ["const stack: string[] = []", "const pairs: Record<string, string>", "stack.pop()"],
        "forbidden_snippets": ["return false\n}"]
      }
    }
  },
  {
    "id": "max-subarray",
    "title": "最大子数组和",
    "source": "interview-seed",
    "summary": "返回连续子数组的最大和。",
    "description": "请实现 maxSubArray(nums)，返回数组中具有最大和的连续子数组的和。",
    "input_description": "nums 为至少包含一个整数的数组。",
    "output_description": "返回最大和。",
    "examples": [
      {
        "input": "nums = [-2,1,-3,4,-1,2,1,-5,4]",
        "output": "6"
      }
    ],
    "starter_code": {
      "javascript": "export function maxSubArray(nums) {\n  // TODO\n  return 0\n}\n",
      "typescript": "export function maxSubArray(nums: number[]): number {\n  // TODO\n  return 0\n}\n"
    },
    "allowed_languages": ["javascript", "typescript"],
    "checks": {
      "javascript": {
        "required_snippets": ["let current = nums[0]", "let best = nums[0]", "best = Math.max(best, current)"],
        "forbidden_snippets": ["return 0"]
      },
      "typescript": {
        "required_snippets": ["let current = nums[0]", "let best = nums[0]", "best = Math.max(best, current)"],
        "forbidden_snippets": ["return 0"]
      }
    }
  }
]
